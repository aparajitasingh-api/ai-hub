# LiteLLM config for local docker-compose testing.
# Points to the llama-server container by service name.
model_list:
  - model_name: qwen-local
    litellm_params:
      model: openai/qwen-local
      api_base: http://llama-server:8080/v1
      api_key: none
