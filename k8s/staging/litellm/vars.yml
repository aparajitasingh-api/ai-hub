# Staging environment - litellm proxy
# Used with: k8s/litellm-deployment.yml

# -- Identity --
NAMESPACE: llm-server

# -- Image --
LITELLM_IMAGE: ghcr.io/berriai/litellm:main-latest

# -- Server --
LITELLM_PORT: 4000

# -- Scaling --
REPLICAS: 2

# -- Resources --
RESOURCES_CPU_REQUEST: 250m
RESOURCES_MEMORY_REQUEST: 512Mi
RESOURCES_CPU_LIMIT: "1"
RESOURCES_MEMORY_LIMIT: 1Gi

# -- Health checks --
READINESS_INITIAL_DELAY: 10
READINESS_PERIOD: 5

# -- Upstream model routing --
# Each model_name maps to an ollama-server service in the same namespace.
# Staging runs a single 14b model.
LITELLM_CONFIG: |
    model_list:
      - model_name: qwen-local
        litellm_params:
          model: openai/qwen-local
          api_base: http://ollama-server-14b:8080/v1
          api_key: none
