apiVersion: v1
kind: ConfigMap
metadata:
  name: litellm-config
  namespace: llm-server
data:
  config.yaml: |
    model_list:
      - model_name: qwen-local
        litellm_params:
          model: openai/qwen-local
          api_base: http://llama-server:8080/v1
          api_key: none
