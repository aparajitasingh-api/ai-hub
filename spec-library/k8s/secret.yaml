apiVersion: v1
kind: Secret
metadata:
  name: spec-library-secrets
type: Opaque
stringData:
  # Replace with your actual GitHub token before applying.
  # In production, use sealed-secrets or an external secret manager instead.
  github-token: "ghp_your-token-here"
