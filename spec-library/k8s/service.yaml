apiVersion: v1
kind: Service
metadata:
  name: spec-library
  labels:
    app: spec-library
spec:
  type: ClusterIP
  selector:
    app: spec-library
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
